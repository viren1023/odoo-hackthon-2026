# React + Vite Frontend

This is the frontend for the FastAPI + React Starter.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Run the development server:
   ```bash
   npm run dev
   ```

The frontend runs on `http://localhost:5173`.
